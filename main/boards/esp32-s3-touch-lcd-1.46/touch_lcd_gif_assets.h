#pragma once

#include <libs/gif/lv_gif.h>

// Declare GIF assets for S3 Touch (reuse electron-bot assets)
LV_IMAGE_DECLARE(staticstate);  // Neutral
LV_IMAGE_DECLARE(happy);       // Happy
// Add more if needed
